# Suggested repository placement

Copy this folder's contents into:

`docs/life-code/diet-sys/`

Recommended prompt references should use these exact filenames.
Do not rename files in the implementation prompt unless you also update the reference map.
